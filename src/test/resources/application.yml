spring:
  data:
    mongodb:
      host: localhost
      port: 0
      database: local

feign:
  retry:
    period: 1000
    max-period: 10000
    max-attempts: 5

oauth:
  google:
    client-id: test
    client-secret: test
    redirect-url: test

auth:
  jwt:
    secret-key: test
    access-exp: 1000
    refresh-exp: 2000

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5