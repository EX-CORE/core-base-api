spring:
  data:
    mongodb:
      uri: mongodb+srv://${DB_USER}:${DB_PASS}@core-base-cluster.r5c8zyj.mongodb.net/core-base-cluster?retryWrites=true&w=majority
  jackson:
    property-naming-strategy: SNAKE_CASE
    default-property-inclusion: non_null